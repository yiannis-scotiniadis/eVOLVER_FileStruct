# piwheels provides pre-built armv6l/armv7l wheels for Raspberry Pi.
# Falls through to PyPI for x86_64 and aarch64 / pure-Python packages.
--extra-index-url https://www.piwheels.org/simple

flask==3.1.3
flask-socketio==5.6.1
pyserial==3.5
# numpy: major version locked for API stability, minor floats so pip can pick
# a version with a wheel for the target Python. numpy 1.25 had no cp313 wheel
# (Raspberry Pi OS Trixie ships Python 3.13); 2.x has cp311-cp313 aarch64
# wheels. Verified: all 8 test suites pass on numpy 2.4.6.
numpy>=2.1,<3
